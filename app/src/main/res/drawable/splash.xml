<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/black" />
    <!-- Añadimos mucho margen para que en versiones antiguas no se vea gigante -->
    <item
        android:drawable="@drawable/ic_metrox_splash_final"
        android:top="150dp"
        android:bottom="150dp"
        android:left="100dp"
        android:right="100dp"
        android:gravity="center" />
</layer-list>
